import React from "react";

type HotelLayoutProps = {
  children: React.ReactNode;
};

const HotelLayout = ({ children }: HotelLayoutProps) => <main className="">{children}</main>;

export default HotelLayout;
